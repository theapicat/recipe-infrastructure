[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    mutating: oppretter, endrer eller sletter data (kjøres kun når API_TEST_ENV er local eller test)
    emails: utløser e-post via notification-service (kjøres kun når API_TEST_ENV er local eller test)
